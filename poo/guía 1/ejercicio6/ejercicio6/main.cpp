#include <iostream>

using namespace std;

/*
 * a) escriba una funcion c++ que determine el interes a obtener por un deposito de dinero en un banco a interes compuestos.
 * se conoce el periodo en meses (n), la tasa de interes mensual(i) y el monto inicial o capital a depositar (c).
 *
 * b) utilice la funcion en un programa que permita el ingreso del monto inicial del deposito y la tasa de interes e informe
 * para distintos periodos (2 a 24 meses) el total que debe reintegrar el banco.
 */


int main()
{






    return 0;
}
